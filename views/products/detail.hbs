<section class="container">

    <article class="portada" style="background-image:url('{{this.photos.[0]}}')"></article>

                    <div class="card-image">
                  <img class="resizeimg" src="{{this.photos.[0]}}" alt="Card image cap">
                </div>
    <h2>{{this.name}}</h2>
    <p>
        {{this.desc}}
    </p>
    <h3>$ {{this.price}}.00 MXN</h3>
</section>

<section class="container">
    <h2>Comentarios:</h2>
    {{#each product.reviews}}
    <p>{{this.user.username}} - {{this.body}}</p>
    {{else}}
    <h3>Aún no hay comentarios</h3>
    {{/each}}
    <form action="/products/{{product._id}}" method="post">
        <input type="text" placeholder="tu nombre" name="name"/>
        <br>
        <textarea name="body" id="" cols="30" rows="10"></textarea>
        <br>
        <input type="submit">
    </form>
</section>